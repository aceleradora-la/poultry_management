<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Eliminar por completo la extensión en Ajustes > Inventario (active=False no bastaba: el arch
         seguía en BD y rompía la validación de res.config.settings al instalar otros módulos). -->
    <delete model="ir.ui.view" id="res_config_settings_view_form_poultry_stock_dashboard"/>
</odoo>
